#include <stdio.h>

// 1-7: Write a program that prints the value of EOF.
// EOF is defined as an integer (-1), so need to print it appropriately.

int main()
{
    printf("%d\n", EOF);
    return 0;
}